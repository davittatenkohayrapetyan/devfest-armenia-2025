# Base path for deployment (e.g., for WordPress subfolder deployment)
# Default is "/devfest2025/" for subfolder deployment
# For root deployment: VITE_BASE_PATH=/
VITE_BASE_PATH=/2025/

# Google Photos Album Feed URL (JSON feed from proxy with CORS enabled)
# Expected JSON schema: { items: [{ id, src, width, height, alt?, caption? }], nextPageToken? }
VITE_GOOGLE_PHOTOS_FEED_URL=

# Partner form URL for partnership inquiries
# Fallback: mailto:gdgyerevan@gmail.com
VITE_PARTNER_FORM_URL=

# Volunteer form URL for volunteer sign-ups
# Fallback: mailto:gdgyerevan@gmail.com
VITE_VOLUNTEER_FORM_URL=
